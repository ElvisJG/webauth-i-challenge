// When i seeded the passwords i seeded them before sending them through bcrypt
// I want to use this script to bcrypt all the passwords in the database
const { FindAllPasswords } = require('./users/users-model.js');

encrypt = () => {};
